name: FTP CAVE
on:
  push:
    branches:
      - main
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: ${{ secrets.FTP_SERVER_CAVE }}
        username: ${{ secrets.FTP_USERNAME_CAVE }}
        password: ${{ secrets.FTP_PASSWORD_CAVE }}
